1)ls-  it will represent whole list 
2)mkdir folder_name   -to create a new folder
3)cd folder_name   -to enter to that new folder
    i)cd --help -it will show how many command can run with cd
    ii)
    iii)
    iv)
    v)
4) cd ..  -to come backfrom the folder
5) rmdir folder_name - to delete the folder
6) echo "text" >>file.js - to create a file in folder
7) pwd- it can show us present working directry puri location bata dega
8) git init -for initialisation of git in folder
9) git status-it will show deleted file and untrack file in red and traked file in green
10) git add file_name/.(it will add all file)  - that file will turn red to green and it will add to git and it is ready to push after comit it
11) rm file_name - it will remove the file
12) git commit -m "kuch bhi message de sakte hai"
13) git log - commit jo jo keya hai bata dega kon kon si commit hui hai
14) git remote add origin link - ye git ko git hub se connect karega
15)git branch -M main - can make new branch or can connect with branch
14) i) git checkout - it will show in which branch it is
    ii) git checkout -b branch_name -  it will made and shift the branch to the given branch name
15)git pull- we can pull the file grom github to local for merging
16)git clone https link of that repo - jo repo hame chaiye vo clone ho jayega or file to git se local me download karne ke leye
17)git push -u origin main -push the file in GitHub this is the final step


**different between git add . and git add *
star will not add hidden folder or hidden file
but git add . will add all the things and it will take record




